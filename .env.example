STT_URL=http://localhost:8000
PORT=8787
VITE_DEBUG=0

# STT service config (used by docker-compose or when running stt/ directly)
WHISPER_MODEL=base
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8
STT_TIMESTAMPS=segments
